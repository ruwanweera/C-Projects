namespace TestProject;

public class Designation
{
    public static void PrintDesignation(string name)
    {
        //This is a static method . No need of define the instance of the object. 
        Console.WriteLine("Designation is : "+ name);
    }
} 
